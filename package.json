{
  "name": "cut-pm-pmp-learning",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test scripts/build-mindmaps.test.mjs",
    "build": "node scripts/build-mindmaps.mjs",
    "verify": "npm test && npm run build && sh scripts/verify-pmp-learning.sh"
  },
  "devDependencies": {
    "mermaid": "^11.12.0"
  }
}
